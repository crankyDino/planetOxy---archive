export function logging() {
  console.log("heeeey, I'm logging here test");

  function getActivePath() {
    const path:string = window.location.href;
    // const nav:Array<string> = document.getElements()
  }
}